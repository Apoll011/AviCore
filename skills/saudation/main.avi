fn intent_hi(intent: Intent) {
    if (get_setting("mode") == "eco") {
        println(locale("light_error"))
    }
    else {
        println("Hello Tiago!")
    }

    println(functions())
}

fn main() {
    println(" Skill Loaded!")
}
