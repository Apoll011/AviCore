fn intent_hi(intent: Intent, ctx: SkillContext) {
    println("Hello Tiago!")
    println(get_constant("API_TOKEN", ctx))
    println(get_setting("mode", ctx))
    println(ctx)
    println(locale("light_error", ctx))
}

fn main() {
    println("Skill Loaded!")
}
