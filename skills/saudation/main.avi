use setting as setting
use locale as locale
use constant as constant
use test as test

fn intent_hi(intent: Intent) {
    error_str := locale::locale("light_error")

    if (test::is_eco()) {
        println(error_str)
    }
    else {
        println("Oi Tiago!")
    }
}

fn main() {
    println("Skill Loaded!")
}