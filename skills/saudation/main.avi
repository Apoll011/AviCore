use setting as setting
use locale as l
use constant as constant
use test as test

fn intent_hi(intent: Intent) {
    error_str := l::get("light_error")

    if (test::is_eco()) {
        say(error_str)
    }
    else {
        say("Oi Tiago!")
    }
}

fn main() {
    println("Skill Loaded!")
}