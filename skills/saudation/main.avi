use setting as setting
use locale as locale
use constant as constant
use test as test
use dialogue as d

fn intent_hi(intent: Intent) {
    if (test::is_eco()) {
        say(locale::get("hello"))
        say(locale::get("ask"))
        d::on_reply_bool("reply", d::bool_validator(false))
    }
    else {
        say("Oi!")
    }
}

fn reply(response) {
    say(response)
}

fn main() {
    println("Greeting Skill Loaded!")
}