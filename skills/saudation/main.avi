fn intent_hi(intent: Intent, cfg: SkillConfig) {
    println("Hello Tiago!")
    println(get_constant("API_TOKEN", cfg))
    println(get_setting("mode", cfg))
}

fn main() {
    println("Skill Loaded!")
}
