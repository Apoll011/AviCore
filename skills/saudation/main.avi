use setting as setting
use locale as locale
use constant as constant
use test as test

fn intent_hi(intent: Intent) {
    if (test::is_eco()) {
        say(locale::get("hello"))
    }
    else {
        say("Oi!")
    }
}

fn main() {
    println("Greeting Skill Loaded!")
}