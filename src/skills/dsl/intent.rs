use dyon::{Module};

pub fn add_functions(module: &mut Module) {
    module.ns("intent");
    /*module.add(Arc::new("require".into()), json_parse, Dfn::nl(vec![Str], Any));
    module.add(Arc::new("optional".into()), json_stringify, Dfn::nl(vec![Any], Str));
    module.add(Arc::new("get".into()), dir, Dfn::nl(vec![], Str));
    module.add(Arc::new("get_raw".into()), dir, Dfn::nl(vec![], Str));
    module.add(Arc::new("full".into()), dir, Dfn::nl(vec![], Str));
    module.add(Arc::new("exists".into()), dir, Dfn::nl(vec![], Str));
    module.add(Arc::new("assert_equal".into()), dir, Dfn::nl(vec![], Str));
    module.add(Arc::new("assert_in".into()), dir, Dfn::nl(vec![], Str));
    module.add(Arc::new("assert_in_dict".into()), dir, Dfn::nl(vec![], Str));*/
}